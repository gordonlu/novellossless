pub struct KnowledgePackLoader;

impl KnowledgePackLoader {
    pub fn new() -> Self {
        Self
    }
}

impl Default for KnowledgePackLoader {
    fn default() -> Self {
        Self::new()
    }
}
